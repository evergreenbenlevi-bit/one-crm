# ONE-CRM — תקן כתיבת משימות ופרויקטים
> גרסה 1.0 | עודכן: 2026-05-01 | בעלות: בן לוי

---

## 1. שפה וסגנון

**חוק:** כל משימה נכתבת **בעברית בלבד**.

### פורמט כותרת: פועל + מושא + הקשר
❌ "פגישה"  
❌ "ONE Course"  
❌ "Email"  
✓ "לסיים את הצעת המחיר לעופר לפני יום חמישי"  
✓ "לשלוח את email ה-onboarding לסטודנטים שנרשמו השבוע"  
✓ "לסקור את ה-draft של מודול 3 בקורס ONE"  

**כלל 1 מינוט:** אם הכותרת לא ברורה מבלי לקרוא את התיאור — תכתוב אחרת.

---

## 2. שדות חובה לכל משימה

| שדה | ערכים | הסבר |
|-----|-------|-------|
| `title` | טקסט | פועל + מושא + הקשר (ראה סעיף 1) |
| `description` | טקסט | ראה תבנית בסעיף 3 |
| `owner` | `ben` / `claude` / `codex` / `both` / `evyatar` | מי מבצע — לא "מי אחראי" |
| `priority` | `p0` / `p1` / `p2` / `p3` | ראה סעיף 4 |
| `due_date` | `YYYY-MM-DD` | **תמיד**, גם אם "גמיש" — בחר תאריך ריאלי |
| `status` | `open` / `in_progress` / `waiting` / `done` | מצב נוכחי |

### שדות מומלצים (חובה אם רלוונטי)
| שדה | מתי להשתמש |
|-----|-----------|
| `project_id` | **חובה** אם קיים פרויקט רלוונטי — כל משימה שהיא חלק מפרויקט |
| `parent_id` | אם זו תת-משימה של משימה אחרת |
| `estimated_minutes` | אם יודעים כמה זמן לוקח — עוזר לתכנון |
| `tags` | קטגוריה נוספת, שם איש קשר, שם פרויקט, לאינדוקס מהיר |

---

## 3. תבנית תיאור — הפרומפט המדויק

```
**מה:** [פעולה ספציפית אחת — מה בדיוק לעשות]
**למה:** [ה-outcome שמשיגים — מה משתנה כשזה נגמר]
**Done when:** [קריטריון ברור לוידוא — איך יודעים שנגמר]
**הקשר:** [כל מה שחסר בלי לקרוא — links, IDs, files, names]
```

### דוגמה מלאה:
```
**מה:** לשלוח email follow-up לשלושת הלידים שלא ענו מהשבוע שעבר
**למה:** הם ביקשו מידע על ONE — אם לא נחזור נאבד את ההזדמנות
**Done when:** 3 emails נשלחו, מסומנים כ-"contacted" ב-CRM
**הקשר:** לידים: אורי כהן, מיה לוי, רון שמיר — ב-leads table. 
          Template ב-/Documents/email-templates/follow-up-v2.md
```

---

## 4. מדריך עדיפויות

| עדיפות | משמעות | דוגמה |
|--------|---------|-------|
| **P0 — אש** 🔴 | ייכשל משהו אם לא עכשיו | Server down, deadline היום, client מחכה |
| **P1 — חשוב** 🟠 | השפעה גדולה, השבוע | השקת קמפיין, תיקון bug קריטי |
| **P2 — רגיל** 🔵 | עבודה שוטפת | כתיבת תוכן, פיצ'ר חדש |
| **P3 — קטן** ⚪ | nice-to-have, יום-יום | ניקוי, תיעוד, שיפורים קטנים |

**כלל:** ברירת מחדל = P2. P0 = אירוע חריג, לא יומיומי.

---

## 5. מבנה היררכיה

```
פרויקט
  └── משימה
        └── תת-משימה (רמה אחת בלבד)
```

### פרויקט
- Goal גדול עם deadline ו-owner ברורים
- דוגמה: "השקת ONE Course Q2 2026", "בניית CRM Phase 2"
- סטטוסים: `active` / `paused` / `done` / `archived`

### משימה
- Unit עבודה: **15–120 דקות** (יותר = יש לפרק)
- שייכת לפרויקט אחד או עצמאית
- תמיד עם due_date ו-owner

### תת-משימה
- Step פרטני, תלוי במשימת האב
- Max **4 תת-משימות** לכל משימה
- אם צריך יותר מ-4 — פרק את המשימה לשתיים

---

## 6. Owner — מי עושה מה

| owner | משמעות |
|-------|---------|
| `ben` | בן עושה ידנית — פגישה, שיחה, החלטה, עבודה פיזית |
| `claude` | Claude מבצע אוטומטית — קוד, כתיבה, מחקר, אוטומציה |
| `codex` | ⚠️ **חסום ב-DB — עדיין לא שמיש.** Codex ביצע כ-MCP subagent תחת Claude — רק ריפקטור מכני עם אימות אוטומטי. הפלט עובר design-review + stage-qa לפני merge. ראה `CLAUDE.md` → Agent Role Split |

> **⚠️ `codex` דורש מיגרציה לפני שימוש (2026-08-06).**
> `owner` נאכף ברמת ה-DB, לא רק כאן:
> - `tasks` — Postgres ENUM `task_owner` (`20260316_tasks.sql:7`, שמות עודכנו ב-`20260429100000`)
> - `projects` — `projects_owner_check` (`20260505000000_master_hierarchy.sql:19`)
> - `meetings` — CHECK נפרד עם ערכים שונים (`ben`/`evyatar`/`shared`)
>
> כתיבה עם `owner='codex'` **תיכשל** עד ש-`ALTER TYPE task_owner ADD VALUE 'codex'`
> וה-CHECK של `projects` יעודכנו. עד אז — השתמש ב-`claude` (Claude אחראי על הפלט בכל מקרה).
| `both` | שיתוף פעולה — Claude מכין, בן מאשר |
| `evyatar` | אביתר אחראי |

---

## 7. Quick Reference — כשיוצרים משימה

```
1. כותרת: פועל + מושא + הקשר (בעברית)
2. owner: מי בפועל עושה את זה?
3. priority: כמה בוער? ברירת מחדל P2
4. due_date: מתי בפועל? לא "יום יום"
5. project_id: זה שייך לפרויקט? → בחר אחד
6. description: מה, למה, done when, הקשר
```

**זמן כתיבה מינימלי: 2 דקות. משימה בלי תיאור = משימה חסרת ערך.**

